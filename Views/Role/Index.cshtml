@model IEnumerable<AppRole>
<!-- IEnumerable değeri List demek ile aynı şeydir -->

@{
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col">
        <div class="shadow" id="KategoriTableSettings">
            <div class=" py-1 d-flex justify-content-between align-items-center" >
                <h5 class="card-title p-2">Roller</h5>
                <a asp-controller="Kategori" asp-action="Create" class="btn btn-sm btn-primary m-2">
                    <i class="fa-solid fa-plus"></i>
                </a>
            </div>
                <div class="table-responsive">
                    <table class="table table-striped mb-0">
                        <thead class="table-secondary">
                            <tr>
                                <th class="text-center">Id</th>
                                <th class="text-center">Role Adı</th>
                                <th style="width: 90px;"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">@item.Id</td>
                                    <td class="text-center">@item.Name</td>
                                    <td>
                                        <a asp-controller="Kategori" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary btn-sm">
                                            <i class="fa-solid fa-pen-to-square"></i>
                                        </a>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
        </div>
    </div>
</div>